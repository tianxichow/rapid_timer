sudo: required

language: c

compiler:
  - clang
  - gcc

before_install:
  - pip install --user cpp-coveralls

script: make coverage=yes 

after_success:
  - coveralls --exclude ./example/sample_demo -b ../ --gcov-options '\-lp '