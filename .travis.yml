sudo: required

language: c

compiler:
  - clang
  - gcc

before_install:
  - pip install --user cpp-coveralls

script: make coverage=yes 

after_success:
  - ./example/sample_demo
  - coveralls --exclude ./example/sample_demo --gcov-options '\-blp'